# -------------------------------------------------------------------
#  Template for secrets.yaml
#
#  1. Copy this file:        cp secrets.yaml.example secrets.yaml
#  2. Fill in real values in secrets.yaml (it is git-ignored).
#  3. NEVER commit the real secrets.yaml.
# -------------------------------------------------------------------

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Generate with:  openssl rand -base64 32
api_encryption_key: "REPLACE_WITH_32_BYTE_BASE64_KEY"

ota_password: "REPLACE_WITH_OTA_PASSWORD"
