# --- ESPHome ---
# Real secrets must NEVER be committed. Copy secrets.yaml.example -> secrets.yaml
secrets.yaml
.esphome/
*.bin
build/
.pioenvs/
.piolibdeps/
# ESPHome auto-generates a .gitignore in each config dir on every compile
simulator/.gitignore

# Python / venv
.venv/
venv/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
